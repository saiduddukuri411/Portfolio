import React from 'react';
import '../styles/card.scss';

const Card = ({title}) => {
    return (
        <div className="card">
            <h7>{title}</h7>
        </div>
    )
}
export default Card;
